<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowMenuForCust"></activity>
        <activity android:name=".LoginAsCust" />
        <activity android:name=".ShowMenu" />
        <activity android:name=".ShowSelection" />
        <activity android:name=".Addmenu" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Selection" />
        <activity android:name=".show" />
        <activity android:name=".activity_register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>